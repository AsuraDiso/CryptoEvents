<?xml version="1.0" encoding="UTF-8"?>
<definitions name="HelloService"
             targetNamespace="http://localhost:5000/hello"
             xmlns="http://schemas.xmlsoap.org/wsdl/"
             xmlns:soap="http://schemas.xmlsoap.org/wsdl/soap/"
             xmlns:tns="http://localhost:5000/hello"
             xmlns:xsd="http://www.w3.org/2001/XMLSchema">

  <!-- 1. СООБЩЕНИЯ (что отправляем и получаем) -->
  <message name="SayHelloRequest">
    <part name="name" type="xsd:string"/>
  </message>
  
  <message name="SayHelloResponse">
    <part name="greeting" type="xsd:string"/>
  </message>

  <!-- 2. ОПЕРАЦИИ (что умеет делать сервис) -->
  <portType name="HelloPortType">
    <operation name="SayHello">
      <input message="tns:SayHelloRequest"/>
      <output message="tns:SayHelloResponse"/>
    </operation>
  </portType>

  <!-- 3. КАК ВЫЗЫВАТЬ (протокол и формат) -->
  <binding name="HelloBinding" type="tns:HelloPortType">
    <soap:binding style="rpc" transport="http://schemas.xmlsoap.org/soap/http"/>
    
    <operation name="SayHello">
      <soap:operation soapAction="SayHello"/>
      <input><soap:body use="literal"/></input>
      <output><soap:body use="literal"/></output>
    </operation>
  </binding>

  <!-- 4. ГДЕ НАХОДИТСЯ СЕРВИС -->
  <service name="HelloService">
    <port name="HelloPort" binding="tns:HelloBinding">
      <soap:address location="http://localhost:5000/hello"/>
    </port>
  </service>

</definitions>